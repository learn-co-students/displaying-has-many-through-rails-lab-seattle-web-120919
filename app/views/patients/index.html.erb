<h2>Patient's Index Page</h2>

    <ul>
<% @patients.each do |patient| %>
    <li> Patient Name: <%= link_to patient.name, patient_path(patient) %><br>
    Number of Appointments: <%= patient.appointments.count %><br></li>
<% end %><br>
    </ul>
